% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/part2_experiments.R
\name{weak_learnability_experiment}
\alias{weak_learnability_experiment}
\title{Weak Learnability Experiment}
\usage{
weak_learnability_experiment(
  gamma_values = c(0.05, 0.1, 0.2, 0.3),
  wave_counts = c(3, 5, 8, 12, 18, 25, 40, 60, 90, 130, 200, 300),
  n_replicates = 200,
  verbose = TRUE
)
}
\arguments{
\item{gamma_values}{Numeric vector of weakness parameters.}

\item{wave_counts}{Integer vector of wave counts to test.}

\item{n_replicates}{Number of Monte Carlo replicates per combination.}

\item{verbose}{Logical; if \code{TRUE} (the default), progress messages
are emitted via \code{\link{message}} and can be suppressed with
\code{\link{suppressMessages}}.}
}
\value{
A data.frame with columns \code{gamma}, \code{waves}, \code{rep},
and \code{accuracy}.
}
\description{
Tests whether gamma-weak ant colonies converge to correct
decisions with sufficient recruitment waves, as predicted by Theorem 4.
Parameters are calibrated so that small gamma is genuinely hard (slow
convergence) while large gamma converges quickly.
}
